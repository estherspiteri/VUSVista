For local docker:
docker-compose up --build

For deployed docker image:
docker buildx build -f Dockerfile --platform linux/amd64 --tag espi0027/vus-curation-system:latest --push .


-- delete all table contents populated by variant uploads
BEGIN;
DELETE FROM variants;
DELETE FROM samples;
DELETE FROM file_uploads;
DELETE FROM phenotypes;
DELETE FROM publications;
DELETE FROM auto_clinvar_updates;
COMMIT;
